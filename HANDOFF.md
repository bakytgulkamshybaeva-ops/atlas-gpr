# HANDOFF · ATLAS · ГПР-движок (atlas-gpr)

Передача проекта для новой сессии. Читай этот файл первым.

---

## 0. АКТУАЛЬНОЕ СОСТОЯНИЕ (2026-06-08) — ЧИТАЙ ПЕРВЫМ

**Это отдельный репозиторий** `bakytgulkamshybaeva-ops/atlas-gpr` (вынесен из muraplan-mockup).
**Live:** https://bakytgulkamshybaeva-ops.github.io/atlas-gpr/ · **локально:** `~/Documents/Claude/atlas-gpr/` (index.html в КОРНЕ). Push: `GIT_SSH_COMMAND="ssh -i ~/.ssh/sales_companion_ed25519 -o IdentitiesOnly=yes" git push` (владелец ops).

**Что построено (всё LIVE):**
- **App-shell:** дерево слева (Портфель/Снабжение → ЖК → блоки, фильтр+поиск) + детали справа. JS: `buildSidebar / viewPortfolio / viewObject / viewBlock / viewSupply`.
- **Реальный ГПР из MS Project** на каждый блок: `mpp/*.html` (39, WBS + связи Finish→Start + критпуть + hover-подсветка), встроены iframe + кнопка «во весь экран» (overlay).
- **Правда из .mpp везде:** готовность, **настоящий критпуть**, прогноз сдачи (вердикт успеваем/просрочка), «что критично сейчас» (просроч. работы критпути по портфелю), системные узкие места (разделы на критпути × блоков). Реф.дата снимка = **28.05.2026**.
- **Снабжение-мост** (📦 в дереве): «заказать-до = старт − срок поставки» для длинных позиций (лифты/металл/окна/двери/слаботочка/насосы/щиты). Lead-времена — ОЦЕНКА, калибровать с PM.

**Данные:** `gpr_data.json` (PDF блок-уровень: план/факт/работы) + константы `MPP{}` (готовность/критпуть/ct) и `SUPPLY[]` — вшиты в `index.html`. Источник .mpp/PDF: `~/Documents/Claude/gpr-source/`.

**Тулинг / пересборка** (`mpp_tools/`):
- `MppExtract.java` — .mpp → JSON через **MPXJ**. ⚠️ ЗАПУСКАТЬ ОТДЕЛЬНЫМ процессом `java` (jpype-в-python падает SIGBUS без JIT-entitlement). Нужен JDK (был в `/tmp/jdk21`; если нет — скачать Adoptium 21 aarch64; `pip install --user mpxj jpype` для jar-ов, путь jar-ов `~/Library/Python/3.9/.../mpxj/lib/*.jar`). Пакет **`org.mpxj`**. Имена .mpp в NFD → `unicodedata.normalize('NFC')`.
- `build_mpp.py` — все .mpp → `mpp/<oid>-<spot>.html` по шаблону. `build_summary.py` — обогащает `MPP{}` + считает `SUPPLY[]`, инжектит в index.html. `parse_gpr.py` — PDF → gpr_data.json.

**ОСТАЛОСЬ (выбрано PM, по порядку):**
1. **Живой движок (приём факта)** — сейчас готовность = статичный снимок из .mpp; дать обновлять факт работы (ручной ввод %, localStorage) → готовность/прогноз/«что критично»/снабжение пересчитываются вживую. Это возврат к исходной идее «живой план↔факт».
2. **Обновление данных одним прогоном** — новые `.mpp`/отчёт → один скрипт пересобирает все блоки + реестр + публикует.
3. Калибровка lead-времён снабжения с PM (или подключить реальный график материалов). Настоящий сетевой CPM УЖЕ есть (из .mpp).

**Gotchas:** preview-песочница флакует/дрейфует — рендерить локально `python3 -m http.server 8810` в atlas-gpr + preview top-nav на `http://127.0.0.1:8810/`. Память: `MEMORY.md → muraplan-mockup-project` (бул про gpr-engine + atlas-gpr).

---

## 1. Что это и зачем
**ГПР как живой движок «план ↔ факт»** — единый источник данных, над которым должны
строиться все ролевые экраны (портфель → объект → участок), бот и снабжение. Не «ещё
один экран», а ядро. Сейчас — браузерный прототип (Фаза 1); цель — реальный сервис (Фаза 2).

## 2. Где
- **Файлы:** `gpr-engine/index.html` (самодостаточный: вшитые данные + движок + UI) · `parse_gpr.py` (PDF→`gpr_data.json`, `--validate`/`--build`) · `gpr_data.json` (1322 работы) · `VALIDATION.md` · `README.md` + этот HANDOFF.
- **Отдельный репозиторий** `bakytgulkamshybaeva-ops/atlas-gpr` (вынесен из muraplan-mockup 2026-06-08 для развязки с параллельной разработкой). `index.html` в КОРНЕ → чистый Pages-URL. Push: SSH-ключ `~/.ssh/sales_companion_ed25519` (владелец ops).
- **Live:** https://bakytgulkamshybaeva-ops.github.io/atlas-gpr/
- **Локально:** `~/Documents/Claude/atlas-gpr/` (index.html и всё — в корне, без подпапки gpr-engine).
- **Прежняя копия** (подпроект) осталась в `dakeshisan/muraplan-mockup/gpr-engine/` — устаревает.
- **Реальный ГПР из MS Project:** `mpp/*.html` (39 блоков, WBS + связи F→S + критпуть) + тулинг `mpp_tools/` (MppExtract.java через MPXJ, build_mpp.py, build_summary.py).
- **Исходные данные (НЕ в репо, public!):** `~/Documents/Claude/gpr-source/` — оригинальный zip + 43 файла (4 PDF-отчёта 28.05.2026 + .mpp MS Project, корректные имена).

## 3. ГЛАВНЫЙ ПРИНЦИП (не нарушать)
«Корректируется исходя из факта» = про **прогноз**, не про план:
- **План (baseline) ЗАФИКСИРОВАН.** Не подгонять под факт — иначе исчезнет отставание.
- **Корректируется прогноз + отставание** при каждом новом факте.
- **Перепланирование (новый baseline) — решение руководства** (директор/PM), не автомат. Движок предлагает — человек утверждает. (KPI-инвариант: план задаётся сверху.)

## 4. Модель данных
```
Портфель → Объект (ЖК) → Очередь (q) → Блок («пятно») → Раздел → Работа
```
Готовность катится снизу вверх. Метод %: взвешенно по длительности работ (как MS Project);
в прототипе — на уровне блока (avg). **«Пятно» = блок, «очередь» = q.**

## 5. Текущий прототип (что уже есть, LIVE)
- `index.html`: массив `OBJECTS` (4 ЖК / 38 блоков), у каждого блока `[имя, план%, факт%]` — **реальные данные отчёта 28.05.2026**.
- Движок: `gf()` (факт), `avg`, rollup по блоку/очереди/объекту/портфелю, отставание = план−факт, прогноз `при +RATE%/нед → 100% через N нед` (RATE=6, **условный темп**).
- UI: дерево объект→очередь→блок; **факт редактируется** (number-инпут) → готовность/отставание/прогноз пересчитываются мгновенно (`recompute()`); факт хранится в `localStorage` (`gpr_fact_v1`); «↺ сбросить факт».
- Проверено в браузере: правка факта блока → live-пересчёт портфеля + персист после reload, без ошибок.

## 6. Источник данных и как его читать
- **PDF-отчёты** `Отчет <ЖК> 28.05.2026.pdf` (в `~/Documents/Claude/gpr-source/ГПР по объектам/`): по каждому блоку шапка `Пятно "X" N очередь` + План%/Факт%, и **по-работная таблица** (37 работ: план%, факт%, начало, окончание, дни, отставание, договор/оплата/материалы, исполнитель).
- **Парсинг PDF:** установлен `pymupdf` (`import fitz`); `pdftotext`/`pdfplumber` — НЕТ. Сводка блока = строка слов, где есть `Пятно "X" N очередь` + два % (План, Факт). Имена в zip — UTF-8 без флага → чинить `name.encode('cp437').decode('utf-8')`.
- **.mpp (MS Project) — бинарные, без спецсофта не парсятся.** Их импорт (→ baseline) — отдельная задача Фазы 2 (нужна Java-библиотека MPXJ или экспорт в XML/Excel).

## 7. Дорожная карта
1. ✅ **Работный уровень + Гантт СДЕЛАНЫ** — `parse_gpr.py` тащит 1322 работы из 4 PDF в `gpr_data.json` (инлайнится в `index.html` через `--build`). **Drill-down блока = диаграмма Гантта** (`buildGantt`): работы как бары на временной оси (начало→окончание), заливка=факт%, вертикальная линия «сегодня» (дата отчёта), месячная ось, sticky-колонка названий с редактируемым фактом, авто-скролл к «сегодня»; критические работы красным, отстающие янтарным. **Узкие места по «Отставанию в днях»** (критический путь портфеля). Сверка — `VALIDATION.md`. ⚠️ блочные План/Факт берём из отчёта (authoritative): из работ не выводятся (см. VALIDATION). № не уникален (слаботочка перезапускает 27,28) → ключ по позиции.
2. **Импортёр MS Project** (`.mpp` → baseline) или периодический экспорт.
3. **Настоящий сетевой CPM** по зависимостям задач из `.mpp` (в PDF зависимостей НЕТ) — отдельный пункт сразу после п.2. Решение PM «оба поэтапно» (2026-06-07): рейтинг узких мест из PDF — сейчас (готов), сетевой критический путь — с импортёром.
4. **Приём факта от бота «Флэш»** (НУ→РП→подтверждение) — реальный поток факта.
5. **API/данные** — чтобы мокап-экраны (portfolio/object/nu/rp) читали из движка (единый источник).
6. **БД факта**, аудит-след изменений, процесс согласования перепланирования.
> Образец архитектуры запускаемого ядра — `supply_core` (Python) в проекте снабжения (см. память).
> Системные узкие места портфеля (≥30 дн): Металлоконструкции 14×, Гидроизоляция кровли 14×, Двери 12×, АПС 10×, Лифты 8× — кандидаты на стык со «Снабжением» (один work_uid → один виновник).

## 8. Как смотреть / публиковать
- Локально: `python3 -m http.server 8800 --bind 127.0.0.1` в `gpr-engine/` → `http://127.0.0.1:8800/`.
- Публикация — вместе с репо мокапов (push в `main` → Pages). При выносе в отдельный репозиторий — настроить свой Pages.

## 9. Старт новой сессии
1. Прочитай этот HANDOFF + `README.md`.
2. Память (`MEMORY.md` → `muraplan-mockup-project`, раздел про ГПР-движок и реальные данные) содержит детали и цифры.
3. Открой `index.html` локально, поменяй факт — увидишь пересчёт.
4. Первая ценная задача — раздел 7, пункт 1 (работный уровень + критический путь из PDF).

> Данные — отчёт 28.05.2026, рабочие. ATLAS · ATAMŪRA GROUP.
